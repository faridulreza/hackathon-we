import React from "react";
import { useState } from "react";

const Home = () => {
  return (
    <div>
      {" "}
      "signed up" <button onClick={signOut}> signout</button>
    </div>
  );
};

export default Home;
